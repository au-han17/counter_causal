# R-13-full — Llama-3.1-8B, LoCoMo multi-hop (282), full, cache 15000
# Paper Table 2 target: n/a — anomaly probe, not a paper number
#
# LoCoMo category 2 == multi-hop. Confirm the filtered count is 282 on first run;
# if it differs, the slice definition needs revisiting before comparing anything.
# chunk_size left unset => cache_size//4 = 3750. The ledger does not specify it.
# recent_size omitted on purpose: build_hook applies the upstream defaults
# (0 for counter-causal variants, cache_size//2 for h2o), per README "Key arguments".
task: locomo
model: meta-llama/Llama-3.1-8B-Instruct
dtype: float16
hook: none
category: 2
seed: 0
