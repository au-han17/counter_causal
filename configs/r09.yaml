# R-09 — Qwen2.5-7B, MATH500 (500 problems), J=512, h=256, max 2048 new tokens
# Paper Table 2 target: .744
#
# recent_size omitted on purpose: build_hook applies the upstream defaults
# (0 for counter-causal variants, cache_size//2 for h2o), per README "Key arguments".
task: math500
model: Qwen/Qwen2.5-7B-Instruct
dtype: float16
hook: counter_causal
cache_size: 512
chunk_size: 256
max_new_tokens: 2048
seed: 0
